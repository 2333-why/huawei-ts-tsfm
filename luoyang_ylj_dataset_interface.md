# 洛阳与 YLJ 光伏数据集说明



## 2. 统一数据形式

洛阳和 YLJ 最终都整理为一个 Parquet 文件。基本组织方式为：

- 一行代表一个功率时间点；
- `timestamp` 是该行数据的时间；
- 功率、实测气象和预报气象以普通数值列保存；
- 洛阳图像以“路径列表+图片时间戳列表”保存；
- YLJ 没有图像模态；



## 3. 洛阳数据集

### 3.1 文件位置与全量时间范围

当前 Parquet 文件位置为：

```text
/data/PVMMoE/DATA/01-Solar/Luoyang-XS/Benchmark_V1/Luoyang-Unified_format-V1.parquet
```

当前图片根目录为：

```text
/home/ma-user/work/john/IMAGE/Luoyang/Luoyang_EKO_ASI_resized_64
```

全量 Parquet 的检查结果如下：

| 项目 | 结果 |
| --- | --- |
| 最早时间 | `2025-04-11 00:00:00` |
| 最晚时间 | `2026-06-11 23:55:00` |
| 总时间跨度 | 427 个完整自然日，约 14 个月 |
| 总行数 | 122,976 |
| 采样颗粒度 | 5 分钟一行 |
| 理论行数 | `427 × 24 × 12 = 122,976` |
| 重复时间戳 | 0 |
| 无法解析的时间戳 | 0 |
| 文件内时间是否递增 | 是 |
。

### 3.2 全部字段

洛阳 Parquet 共有 18 个字段：

| 字段 | Parquet 类型 | 类别 | 含义 |
| --- | --- | --- | --- |
| `timestamp` | `timestamp[ns]` | 时间 | 当前行对应的功率时间 |
| `final_power` | `double` | 功率 | 光伏实际功率，也是预测目标 |
| `asi_path` | `list<string>` | 图像 | 当前 5 分钟内的天空图相对路径列表 |
| `asi_path_timestamps` | `list<timestamp[us]>` | 图像时间 | 与 `asi_path` 逐项对应的图片拍摄时间 |
| `GHI_mean_observe` | `double` | 实测 | 实测平均全球水平辐照度 |
| `msl_observe` | `double` | 实测 | 实测平均海平面气压 |
| `t2m_observe` | `double` | 实测 | 实测 2 米气温 |
| `u10_observe` | `double` | 实测 | 实测 10 米风速 U 分量 |
| `v10_observe` | `double` | 实测 | 实测 10 米风速 V 分量 |
| `u100_observe` | `double` | 实测 | 实测 100 米风速 U 分量 |
| `v100_observe` | `double` | 实测 | 实测 100 米风速 V 分量 |
| `GHI_mean_forecast` | `double` | 预报 | 预报平均全球水平辐照度 |
| `msl_forecast` | `double` | 预报 | 预报平均海平面气压 |
| `t2m_forecast` | `double` | 预报 | 预报 2 米气温 |
| `u10_forecast` | `double` | 预报 | 预报 10 米风速 U 分量 |
| `v10_forecast` | `double` | 预报 | 预报 10 米风速 V 分量 |
| `u100_forecast` | `double` | 预报 | 预报 100 米风速 U 分量 |
| `v100_forecast` | `double` | 预报 | 预报 100 米风速 V 分量 |

### 3.3 单位说明

- `final_power` 当前按 kW 使用。
- 电站额定功率记录为 `48629.73 kW`。
- 根据数值范围，`msl`、`t2m`、`u/v` 很可能分别采用 Pa、K、m/s，`GHI` 通常采用 W/m²，

### 3.4 数值字段缺失情况

| 字段或字段组 | 缺失行数 | 缺失比例 | 情况 |
| --- | ---: | ---: | --- |
| `final_power` | 0 | 0% | 功率目标完整 |
| `GHI_mean_observe` | 21,891 | 17.80% | 部分缺失 |
| `msl_observe` | 110,723 | 90.04% | 严重缺失 |
| `t2m_observe` | 110,723 | 90.04% | 严重缺失 |
| `u10_observe` | 110,723 | 90.04% | 严重缺失 |
| `v10_observe` | 110,723 | 90.04% | 严重缺失 |
| `u100_observe` | 110,723 | 90.04% | 严重缺失 |
| `v100_observe` | 110,723 | 90.04% | 严重缺失 |
| `GHI_mean_forecast` | 6,967 | 5.67% | 少量缺失 |
| 其余 6 个预报气象字段 | 每列 1,772 | 每列约 1.44% | 完整度较高 |

完整文件中的功率数据是连续的，但部分实测气象字段只有很短时间段存在。算法设计者不能仅根据字段存在就认为该字段全年可用，必须同时检查字段覆盖率。

### 3.5 图片的组织形式

洛阳 Parquet 每个 5 分钟行中通常预留 5 个一分钟图片位置。例如功率行时间为 `00:00` 时，对应图片时间通常为：

```text
23:56, 23:57, 23:58, 23:59, 00:00
```

图片字段遵守以下规则：

- `asi_path` 和 `asi_path_timestamps` 都是列表；
- 两个列表的相同下标代表同一张图片；
- 图片路径是相对路径；
- 图片绝对位置为 `image_root / asi_path中的相对路径`；
- 图片不需要与 Parquet 放在同一目录；
- 空路径表示该图片位置缺失；
- 已检查数据中路径列表和时间列表长度一致；
- 已检查数据中没有晚于对应功率行时间的未来图片。

### 3.6 全量图片缺失情况

| 项目 | 数量或比例 |
| --- | ---: |
| 理论图片槽数量 | 614,880 |
| 存在路径和时间信息的图片槽 | 97,213（15.8101%） |
| 空路径图片槽 | 517,667（84.1899%） |
| 5 张图片全部存在的功率行 | 19,385（15.7632%） |
| 5 张图片全部缺失的功率行 | 103,517（84.1766%） |
| 部分图片缺失的功率行 | 74（0.0602%） |

最长的连续无图片元数据区间为：

```text
2025-04-11 00:00:00
至
2026-04-05 05:20:00
```

也就是说，洛阳全量文件虽然包含一年以上的功率和气象数据，但前面接近一年基本没有可用图片。

### 3.7 当前只使用约两个月实验

当前实验选择以下时间：

| 用途 | 时间范围 |
| --- | --- |
| 训练和验证数据范围 | `2026-04-05 00:00:00` 至 `2026-05-10 23:55:00` |
| 测试数据范围 | `2026-05-11 00:00:00` 至 `2026-06-11 23:55:00` |

使用这一段约两个月的数据，不是因为 Parquet 只有两个月，而是因为这段时间才具有连续且较完整的图片。

测试区间的检查结果为：

- 共 9,216 个五分钟功率行；
- 9,156 行的 5 张图片全部存在；
- 只有 25 行完全没有图片；
- 图片槽总体可用率为 `45,916 / 46,080 = 99.6441%`。



## 4. YLJ 数据集

### 4.1 文件与基本形式

当前 Parquet 文件位置为：

```text
/data/PVMMoE/DATA/01-Solar/YLJ/Benchmark/YLJ-Unified_format.parquet
```

YLJ 数据集具有以下特点：

- 一行对应一个 15 分钟时间点；
- 功率字段为 `observe_power`；
- 功率单位为 MW；
- 电站额定功率当前记录为 `468 MW`；
- 包含实测气象、1-day 预报和 4-hour 预报；
- 没有图片字段，也没有图片模态。

### 4.2 全部字段

YLJ 约定共有 23 个字段：

| 字段 | 类别 | 含义 |
| --- | --- | --- |
| `timestamp` | 时间 | 当前行数据的时间戳 |
| `observe_power` | 功率 | 实测功率，也是预测目标 |
| `GHI_observe` | 实测 | 全球水平辐照度 |
| `TEMP_observe` | 实测 | 温度 |
| `WS_observe` | 实测 | 风速 |
| `WD_observe` | 实测 | 风向 |
| `PREC_observe` | 实测 | 降水 |
| `PWAT_observe` | 实测 | 可降水量，源单位 mm |
| `SDWE_observe` | 实测 | 雪水当量 |
| `GHI_forecast_1day` | 1-day 预报 | 全球水平辐照度 |
| `TEMP_forecast_1day` | 1-day 预报 | 温度 |
| `WS_forecast_1day` | 1-day 预报 | 风速 |
| `WD_forecast_1day` | 1-day 预报 | 风向 |
| `PREC_forecast_1day` | 1-day 预报 | 降水 |
| `PWAT_forecast_1day` | 1-day 预报 | 可降水量，源单位 mm |
| `SDWE_forecast_1day` | 1-day 预报 | 雪水当量 |
| `GHI_forecast_4hour` | 4-hour 预报 | 全球水平辐照度 |
| `TEMP_forecast_4hour` | 4-hour 预报 | 温度 |
| `WS_forecast_4hour` | 4-hour 预报 | 风速 |
| `WD_forecast_4hour` | 4-hour 预报 | 风向 |
| `PREC_forecast_4hour` | 4-hour 预报 | 降水 |
| `PWAT_forecast_4hour` | 4-hour 预报 | 可降水量，源单位 mm |
| `SDWE_forecast_4hour` | 4-hour 预报 | 雪水当量 |

除功率和 PWAT 外，现有数据说明没有正式给出其他 YLJ 气象字段的物理单位，后续应根据原始数据生产说明补充，不能依靠字段名称自行确定换算方式。

### 4.3 时间范围

当前使用的时间区间为：

| 用途 | 时间范围 |
| --- | --- |
| 训练和验证数据范围 | `2024-01-01 00:00:00` 至 `2024-12-31 23:45:00` |
| 测试数据范围 | `2025-01-01 00:00:00` 至 `2025-05-31 23:45:00` |




## 5. 两个数据集对照

| 项目 | 洛阳 | YLJ |
| --- | --- | --- |
| 文件形式 | 单个 Parquet | 单个 Parquet |
| 时间字段 | `timestamp` | `timestamp` |
| 功率字段 | `final_power` | `observe_power` |
| 功率单位 | kW | MW |
| 额定功率 | 48629.73 kW | 468 MW |
| 时间颗粒度 | 5 分钟 | 15 分钟 |
| 实测气象 | 有 | 有 |
| 气象预报 | 一套 forecast 字段 | 1-day 和 4-hour 两套预报 |
| 图像 | 有，一分钟图片路径列表 | 无 |
| 功率缺失 | 当前检查为 0 | 存在缺失可能 |
| 主要完整性问题 | 早期接近一年没有图片，部分实测气象严重缺失 | 功率、实测和预报均可能缺失，准确比例待检查 |
| 当前训练验证范围 | 2026-04-05 至 2026-05-10 | 2024 全年 |
| 当前测试范围 | 2026-05-11 至 2026-06-11 | 2025-01-01 至 2025-05-31 |

